# Virtual environment - contains installed packages, not source code.
# Anyone cloning the repo recreates it with: python3 -m venv venv && pip install -r requirements.txt
venv/

# Python bytecode cache - auto-generated, machine-specific.
__pycache__/
*.pyc

# Snapshot log - local runtime data built up over repeated runs, not source.
snapshots.csv
